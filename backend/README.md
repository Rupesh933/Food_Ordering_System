CORS = Cross-Origin Resource Sharing
It's a security rule in browsers that blocks requests coming from a different domain/port
React (3000) → asks data → Django (8000)
                    ↓
         ✅ Django says "React is allowed!"
         Data flows freely!